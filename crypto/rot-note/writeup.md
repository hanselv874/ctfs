# Writeup

## Summary

The challenge gives one encoded string and hints that a simple text transform may be enough.

## Approach

`synt` is a common ROT13 form of `flag`, so decode the full string with ROT13.

## Solution

Run `python3 solver/solve.py` from this challenge directory.

## Result

`flag{rot13_is_fun}`
